# Buffer-Correlation Index — Specification v0.2 (FROZEN)
### Pre-registered test of the correlated-drain hypothesis · Written blind to joint data · Phase 2 deliverable (a) · Supersedes v0.1 under the single authorized pre-timestamp revision cycle

**Status:** frozen, awaiting external timestamp (public GitHub commit or OSF registration) before any Phase 3 data contact. v0.1 was frozen but never externally timestamped; a bounded review pass identified defects, and this document is the single permitted revision before the timestamp binds. v0.1's freeze sentence was not violated: it bars edits after the external timestamp, which does not yet exist.
**Provenance chain:** Master Dossier v1 → Red-Team Rebuttal → Arbitration Report → Execution Handoff → Phase 1 Handoff → Phase 2 Kickoff → Spec v0.1 → bounded review pass → **this document.**
**Authorship conditions:** v0.1 was produced in a session with no contact with any buffer-series data; the one authorized exception (retrieval of current and 2025 NY Fed ACM term-premium values, used solely to set the Prediction Slate's P3 threshold) was exercised there and is not repeated here. This v0.2 revision consulted source *documentation* only — series availability, program history, methodology notes — never series values, and worked exclusively from the frozen v0.1/v1.0 files, the Phase 2 kickoff, the execution and Phase 1 handoffs, the arbitration report, and the written review brief. The term premium is not a component of this index. Every v0.2 change is tagged in the change log (§13) with its direction of effect: thesis-helping, null-helping, or neutral-precision.

---

## 0. The claim under test

The framework's mechanism layer asserts, as hypothesis [H]: *US collective buffers have been draining in increasingly correlated fashion over 2000–2026 — cross-domain co-movement of buffer drawdowns has risen over the window — which is the precondition under which a single shock propagates across buffers rather than being absorbed locally.*

This index tests the **precondition** (rising co-drain), not cascade itself. Whether a bound buffer produces cascade or restraint is the province of the Prediction Slate (P2, P5), not of this instrument. A confirming result here establishes only that the correlated-drain precondition is measurably present; a disconfirming result withdraws the correlated-drain claim entirely.

**The binding commitment (Execution Handoff Rule 3; Kickoff Constraint 3), verbatim:**

> *If the pre-specified analysis returns flat or domain-idiosyncratic cross-domain covariance over 2000–2026, the correlated-drain claim is withdrawn from the framework. No hedging into "not yet measurable."*

Operationally (§6): any result short of the full CONFIRM criteria constitutes disconfirmation for the purposes of this commitment. There is no intermediate verdict. The hypothesis may thereafter be tracked forward only through Prediction P6 (out-of-sample), never re-asserted retrospectively over 2000–2026 without a new, versioned, externally timestamped specification.

---

## 1. Scope

- **Geography:** United States only. Cross-country extension is future work.
- **Window:** 2000–2026 nominal, **reaffirmed in v0.2 after a documented evaluation of backward extension to 1985 or 1990** (§13, item 1 — the window is part of the claim's referent and is retained). Observations for 2026 enter only when the full calendar year's underlying data are published (in practice: not before Phase 3 concludes for most annual-native series, and 2026 is a crisis-excluded year under §6 regardless, so the crisis-excluded headline statistic effectively spans 2000–2025).
- **Frequency:** **Annual** for the primary analysis. Justification: four of eight domains are annual-native (fiscal net-interest, health, social, food) and one is sparse-annual (corporate); computing the headline statistic at the lowest common native frequency avoids interpolation, and interpolation is banned in this spec because carrying annual values across quarters deflates within-year variance and manufactures spurious correlation structure. The cost — few observations — is acknowledged in §6 (power note and precision diagnostic) and partially offset by the quarterly companion analysis (§7, S8) on the quarterly-native subset.
- **Domains:** eight quantitative domains (§3), with munitions carried as a qualitative carve-out (§4). Eight sits inside the mandated 7–9 range.
- **Temporal aggregation:** sub-annual native series enter the annual index as calendar-year means of their native-frequency observations (weekly→monthly→annual means). Federal fiscal-year series are mapped to the calendar year containing the majority of the fiscal year (FY *t*, Oct *t−1*–Sep *t* → calendar year *t*). Agricultural marketing years map per the harvest-year convention stated in §3 (a disclosed deviation from the majority rule, with rationale). For the quarterly companion, monthly series enter as quarterly means.

---

## 2. Methodology — provenance and blindness disclosure (opens this section per Kickoff Constraint 6)

**(a)** The author of this specification knew, at spec time, the current (July 2026) levels of several candidate components — the SPR level, the direction of distillate stocks, the fiscal and private-market stress facts — because the project exists precisely because those levels are known to be low; the Phase 1 Handoff, a required input, contains them. Perfect blindness is unattainable here and is not claimed. **(b)** The author did not examine any historical joint series — no time series of any component over 2000–2026, individually or jointly — before freezing the component selection, orientations, normalization, test statistic, block boundaries, crisis windows, or confirm/disconfirm criteria in this document. The single authorized data pull (ACM term premium, for the Prediction Slate's P3) concerns a series that is not an index component. **(c)** The quantity under test — the *trend* in cross-domain covariance of buffer changes over 2000–2026 — is not recoverable from knowledge of current endpoint levels. Knowing that the buffers are simultaneously low in 2026 says nothing about whether their drawdowns co-moved increasingly over the preceding quarter century; that is exactly what makes this freeze meaningful despite (a). A referee will note residual contamination: any macro-literate author "knows" roughly how 2008 and 2020 behaved. The mitigations are mechanical block boundaries (§5), date-defined crisis exclusions fixed before data contact (§6), and Phase 5's explicit mandate to audit this spec for hindsight bias. **(d) v0.2 revision conditions:** this revision was performed under the same blindness regime. The revision session consulted source documentation (series start dates, program histories, methodology notes) but no series values; did not repeat the ACM pull; and added one domain (food, §3) blind to its historical trajectory, carrying the same macro-literate-residual caveat as (c). The revision is legitimate only because the external timestamp had not yet bound; this is the single permitted revision cycle, and Phase 5 audits the v0.1→v0.2 delta with the same hindsight-bias mandate.

---

## 3. Components and feeds (frozen selection)

### 3.0 Inclusion criteria (made explicit in v0.2)

The v0.1 selection applied these criteria implicitly; they are stated here to adjudicate the two v0.2 candidate domains and to bind any future domain change. **Honesty note, stated before a referee does:** criteria written after a selection cannot certify that selection's neutrality — post hoc criteria fit the choices they ratify almost by construction. Their evidentiary value is prospective (they bind v0.2's additions and any future amendment); Phase 5 audits whether they were gerrymandered.

- **K1 — Object class.** The series measures the level, or the signed change-rate, of a stock of collective absorption capacity, with a fixed orientation over the full window. Response variables inversely coupled to the buffer they track — flows that rise *because* the stock fell, the §4 procurement case — are excluded, because their mis-signing corrupts the covariance statistic. Flows with fixed, positively-signed coupling to the buffer stock are admissible under the change-rate clause: in a first-differences statistic, a flow is the direct reading of Δstock, not a proxy for it.
- **K2 — Coverage.** A published public series spans 2000–2026 at its native frequency, without interpolation and without researcher re-derivation or construction.
- **K3 — Stability.** Methodology and series definition are stable over the window, or breaks are documented and handled by the spec's existing rules. Undocumented redefinitions and changing entity boundaries fail this criterion.
- **K4 — Independence.** No shared source aggregate or mechanical construction coupling with an existing domain beyond what is disclosed and covered by a mandatory sensitivity run.
- **K5 — Distinctness.** The domain names a reservoir of absorption capacity not already represented; the mandated 7–9 range implies breadth, not variants of one reservoir.

### 3.1 Component table

| # | Domain | Sub-series | Feed | Native freq. | Orientation (higher = more buffer?) |
|---|---|---|---|---|---|
| 1 | Strategic reserve | (1a) SPR crude stocks, million bbl | EIA Weekly Petroleum Status Report / Monthly Energy Review | weekly | + |
| | | (1b) SPR coverage ratio = SPR stocks ÷ trailing-12-month mean daily crude inputs to refineries | EIA (both series) | weekly/monthly | + |
| 2 | Financial | (2) BIS credit-to-GDP gap, US, total private non-financial sector — **published values only, not re-derived** (Drehmann–Borio–Tsatsaronis one-sided HP, λ=400,000, per BIS WP 355) | BIS credit-gap statistics | quarterly | **−** (positive gap = froth, depleted absorption capacity) |
| 3 | Fiscal | (3a) Federal net interest outlays as % of federal revenues | OMB Historical Tables / CBO | annual (FY) | − |
| | | (3b) Federal debt held by the public as % of GDP | OMB/CBO (annual); FRED quarterly series for the companion | quarterly/annual | − |
| 4 | Corporate | (4a) Aggregate US nonfinancial net-debt/EBITDA | IMF GFSR chapters; S&P Global / rating-agency public summaries | sparse annual | − |
| | | (4b) Aggregate interest coverage ratio | same | sparse annual | + |
| 5 | Household | (5) Personal saving rate | BEA NIPA (Table 2.6 lineage) | monthly | + |
| 6 | Health capacity | (6) Hospital beds per 1,000 population | AHA Hospital Statistics / OECD Health Statistics | annual | + |
| 7 | Social / associational | (7) Union membership rate (% of employed wage and salary workers) | BLS Union Members annual release | annual | + |
| 8 | Food (v0.2 addition) | (8) US grain stocks-to-use: unweighted mean of marketing-year ending-stocks ÷ total-use ratios for corn, all wheat, and soybeans | USDA WASDE / NASS published estimates | annual (marketing year) | + |

**Frozen decisions and disclosed deviations:**

- **1b deviates from the Execution Handoff's letter** ("days-of-net-import-cover"). Reason, technical and disclosed now: the US became a net petroleum exporter mid-window (~2020), so days-of-*net-import* cover is undefined or sign-flipping over part of 2000–2026 and cannot enter a covariance statistic. The substitute — SPR stocks scaled by refinery crude throughput — is a well-defined intensity measure across the whole window and preserves the handoff's intent (a coverage ratio alongside the raw level). This is the only component-level deviation from the work order, and it is forced by arithmetic, not preference.
- **Fiscal proxy:** net-interest/revenue + debt/GDP is a pragmatic simplification. The conceptual anchor is the Ostry–Ghosh–Kim–Qureshi fiscal-fatigue / debt-limit framework (IMF SPN 2010; Ghosh et al., *Economic Journal* 2013): fiscal buffer as distance to a debt limit implied by a fiscal-reaction function. Estimating that reaction function is out of scope (Execution Handoff Limit 5); the proxy measures the same object crudely — how much of the state's absorption capacity is pre-committed — and the simplification is flagged as such wherever the index is reported.
- **Corporate gap rule and mechanical substitution:** GFSR/S&P summaries will yield an incomplete annual series. No interpolation. If, after Phase 3 collection, the corporate domain covers <60% of any analysis block's usable years, the domain switches **for the full window** to the BIS-published US nonfinancial-corporations credit-to-GDP series (orientation −). The switch is mechanical, not discretionary. If it triggers, the drop-corporate sensitivity run (S1) becomes mandatory reporting alongside every headline figure, because a BIS-credit corporate series shares construction with domain 2 and the induced pair correlation is partly artifactual.
- **Health substitution disclosed:** the conceptually preferred series (HHS hospital/ICU occupancy) begins ~2020 and cannot span the window. Beds per 1,000 is the long-window proxy. Known confound, stated now: its secular decline partly reflects the deliberate outpatient shift in care delivery, a technological/organizational trend, not extraction per se. The index measures buffer *level*, agnostic to cause; interpretation of this component must carry the confound.
- **Social-capital choice, pre-committed:** union membership rate, chosen on data-quality grounds — annual, complete, methodologically stable over the full window (the CPS-based series is consistent from 1983 onward) — over the GSS social-trust and neighbor-socializing items, which are biennial and carry a documented 2020–21 survey-mode comparability break. This is explicitly a data-quality-over-construct-validity trade: union density is an organized-associational-capacity proxy, not a measure of social connection at large. Mitigations, both frozen: the mandated drop-this-component run (S1) and a dedicated swap run substituting GSS social trust (S7).
- **Food domain construction (v0.2 addition, #8):** US grain stocks-to-use, defined as the unweighted mean of the marketing-year ending-stocks-to-total-use ratios for corn, all wheat, and soybeans, using USDA WASDE/NASS published estimates — the latest published estimate per marketing year as of Phase 3 retrieval, retrieval-date-logged, no vintage selection. Marketing years map to the calendar year in which they **begin** (harvest-year convention: corn and soybeans, Sep–Aug marketing years, → the year of the September; wheat, Jun–May, → the year of the June). This is a disclosed deviation from §1's federal-FY majority rule, chosen so the three crops' observations are time-aligned within the domain mean; the majority rule would split the crops across adjacent calendar years (wheat to year *t*, corn/soy to *t+1*) and mix offset periods inside one domain. Orientation +. Annual-native; the domain does not enter the quarterly companion (S8). Stocks are predominantly privately held; the index already includes private-sector buffers (household, corporate, financial), and the object is national absorption capacity regardless of holder.

### 3.2 Application of §3.0 to the v0.2 candidates and the incumbent seven (review item 2)

**USDA grain stocks-to-use (food): INCLUDED as domain 8.** K1: a stock-coverage ratio — ending stocks scaled by use — exactly the object class of 1b, with fixed + orientation; not a flow, not a response variable. K2: USDA publishes per-crop ending stocks and total use across the full window and far earlier; no researcher construction beyond the frozen unweighted three-crop mean. K3: NASS/WASDE definitions are methodologically stable over the window; known episodic stocks-report revisions are ordinary measurement noise, not redefinitions. K4: no shared source aggregate with any existing domain (USDA feed, agricultural object). K5: a distinct reservoir — physical food-system absorption capacity — adjacent to but not a variant of the strategic-petroleum domain (different commodity, source, holders). Eight domains remains inside the mandated 7–9.

**NERC planning reserve margin (grid): REJECTED.** K3 fails: the NERC regional-entity map was reorganized repeatedly across the window (ERO standup 2006; RFC's formation from predecessor councils; SPP RE dissolution 2018; FRCC dissolution into SERC 2019), and the margin concept itself changed treatment over time (anticipated vs prospective margins; evolving demand-response and behind-the-meter accounting). K2 also fails: no single published national reserve-margin series spans 2000–2026; constructing one from shifting regional assessments is researcher re-derivation, which K2 bans and which the spec's own domain-2 precedent ("published values only, not re-derived") forbids. Grid adequacy is a real collective buffer and the rejection is a measurement verdict, not a conceptual one; a consistent published series, if one is validated later, is future-work material under a post-timestamp versioned amendment.

**The incumbent seven, reaffirmed against the same criteria.** Domains 1 (stock + coverage ratio), 3 (stock ratios), 6 (stock ratio), 7 (membership-share stock, stable series) pass without comment. Domain 4 passes K1 (stock ratios) and passes K2 only via the frozen mechanical-substitution rule, which is exactly the documented-handling clause K3 anticipates; its K4 exposure under substitution is disclosed above and covered by mandatory S1 reporting. Domain 5, the personal saving rate, is a **flow** and passes under K1's change-rate clause, and the distinction from the excluded procurement proxy must be exact because the two objections rhyme: personal saving *is* the accumulation flow of the household financial buffer — in a first-differences statistic it is the most directly aligned component in the index, a signed reading of the drawdown/build rate — and its coupling to the buffer stock is fixed and positive. Procurement was excluded (§4) not for being a flow but for being an **inversely coupled response variable**: procurement surges *because* stocks fell, so injecting it mis-signs the component. That defect is absent here. A referee will call K1's change-rate clause drafted-to-fit; the answer is that the clause states the actual statistical requirement (fixed-sign coupling to the buffer), and the requirement, not the flow/stock label, is what the covariance statistic needs. Domain 2 is reaffirmed per §3.3.

### 3.3 Credit-gap object-class defense (review item 5)

Why does domain 2 — a detrended, mean-reverting cyclical measure — survive the objection that excluded procurement budgets in §4? Because the procurement exclusion turned on three defects, and the gap has none of them. **(i) Object class:** the credit-to-GDP gap is a state variable — the deviation of a stock ratio from its own estimated capacity trend — not a flow into or out of any stock, and not a response variable; under the Basel III/CCyB architecture it is the institutional measure of how much financial absorption capacity has been consumed by credit froth, which is precisely the buffer reading this index assigns it (orientation −, fixed over the window). **(ii) Sign:** procurement is inversely coupled to the munitions stock (it rises because the stock fell), which mis-signs the component; the gap has a fixed orientation with no inverse coupling — its innovations move *with* froth accumulation, i.e., with buffer depletion under the stated orientation. **(iii) Political-response character:** procurement measures a policy reaction, not a reserve; the gap is a market-outcome aggregate. Mean reversion per se does not disqualify a component from a first-differences correlation statistic — differencing treats all components symmetrically, and what corrupts covariance is sign instability, which the gap does not have. **The honest limitation, disclosed rather than argued around:** the one-sided HP detrending removes secular components by construction, so the gap cannot register slow secular co-drain. In the differences-based primary statistic this is nearly moot (differences are high-frequency objects); in the levels robustness run (S4) it biases domain 2 toward zero contribution — a null-helping asymmetry, accepted and stated here so that an S4 result is read with it. Domain 2 is retained unchanged; no replacement.

---

## 4. Munitions: qualitative carve-out (frozen decision), and the convenience disclosure

**Decision: munitions are excluded from the quantitative index and carried qualitatively.** Three reasons, each sufficient. First, stock levels are classified; the public record consists of intermittent estimates (the CSIS "Last Rounds?" genre) that cannot support a 26-year series. Second, the available continuous proxy — procurement budgets — is a *flow into* the buffer that moves inversely to the stock (procurement surges follow depletion, as the 2026 THAAD/PAC-3 contracts demonstrate); injecting it as if it measured buffer level would put a mis-signed component into the covariance and corrupt the statistic in whichever direction the mis-signing happens to run. Third, a budget series measures political response, not physical reserve, and conflating the two is precisely the category error this framework is trying to stop making.

**The pre-emptive disclosure, in this spec's own voice:** the two buffers most central to the thesis — munitions and social capital — are the least measurable. Munitions are carved out entirely; social capital enters through a proxy chosen for data quality rather than construct fidelity. The index therefore leans on the domains that happen to be measurable, and a critic will say it measures what is convenient. The critic will be right about the composition, and we say it before they do. The consequence is a scope restriction accepted in advance: **a confirming result licenses the correlated-drain claim only over the eight measured domains and cannot be silently extended to munitions or to social capital broadly.** A disconfirming result, by contrast, withdraws the claim entirely — the asymmetry runs against the thesis, and that is the point of a pre-registration written by the side with the burden of proof.

---

## 5. Normalization, aggregation, and the test statistic (frozen)

**Normalization.** Each sub-series is oriented per the table in §3 (series where higher = less buffer are multiplied by −1) and z-scored against its own full-window (2000–2026, available observations) mean and standard deviation. Each domain's series is the unweighted mean of its sub-series z-scores (domains 1, 3, 4; single-sub-series domains pass through). Domain series are re-standardized to unit variance for the display composite only; the correlation statistic is scale-free and uses the domain series directly.

**Missing data.** No interpolation anywhere. First differences are computed only between adjacent available years. Pairwise correlations use pairwise-complete observations. A domain-pair is excluded from a block's average if it has fewer than 5 complete difference observations in that block; a domain is excluded from a block entirely if it covers <60% of that block's usable years. Every exclusion is logged in the Phase 3 results memo.

**The test statistic — choice made in v0.1, carried unchanged.** The primary statistic is computed on **first differences** of the annual domain series (Δz), not levels. Justification: (i) normalized buffer levels are highly persistent, near-integrated series, and correlations of near-integrated series over short windows are dominated by incidental trend alignment — the spurious-regression problem (Granger–Newbold) in miniature; any two secularly declining series correlate strongly in levels regardless of whether their *drawdowns* have anything to do with each other. (ii) The mechanism claim is about synchronized drawdown — shocks and drains hitting multiple buffers in the same periods — and that is a statement about co-movement of *changes*. (iii) The descriptive layer already establishes that levels are jointly low; re-testing that in the guise of a mechanism test would be circular. **Levels are pre-named as the robustness alternate (S4)**, because a referee can fairly counter that differencing discards low-frequency co-drain; both are reported, the differences version is primary and verdict-bearing.

**Statistic S(t or block):** the unweighted mean of the pairwise Pearson correlations across all included domain pairs (28 pairs at full eight-domain strength) of the Δz series, computed over (a) the pre-defined blocks below (headline, verdict-bearing) and (b) trailing 10-year rolling windows (descriptive trajectory and consistency check only). The 10-year window is chosen over 8 to maximize observations per correlation estimate at annual frequency; the 8-year window is a named robustness run (S6). Spearman rank correlation is a named robustness run (S5), relevant because with 6–9 observations per block a single outlier year can dominate Pearson estimates.

**Blocks — mechanical construction, re-derived for the retained window (identical to v0.1).** The difference sample 2001–2026 (26 observations at most) is cut into near-equal thirds by date, with no reference to any data:
- **B1:** 2001–2009 (9 obs)
- **B2:** 2010–2017 (8 obs)
- **B3:** 2018–2026 (9 obs)

The headline contrast is **Δρ = ρ̄(B3, crisis-excluded) − ρ̄(B1, crisis-excluded)**, with B2 reported for shape (monotonicity is informative but not verdict-bearing).

**Inference.** Within each block, a moving-block bootstrap (block length 2 years, resampling year-vectors jointly across domains to preserve cross-sectional structure, 5,000 replications) yields the sampling distribution of ρ̄; the contrast's distribution is formed by independent resampling of the two blocks. The verdict-bearing interval is the one-sided 90% bootstrap confidence bound on Δρ.

---

## 6. Crisis windows, confirm/disconfirm criteria, precision diagnostic, and the power note (frozen)

**Crisis-window rule.** Common macro shocks mechanically spike difference-correlations across all domains at once; the claim under test is a rising trend **between** crises, not the trivial fact that crises are correlated events. Excluded years, defined by date now, with no discretion at Phase 3:

- **E = {2008, 2009, 2020, 2021, 2026}.**
- Justification: 2008–2009 = GFC acute phase (NBER recession Dec 2007–Jun 2009); 2020–2021 = COVID shock plus the extraordinary fiscal/monetary response that mechanically whipsawed the saving rate, credit gap, and fiscal series through 2021; 2026 = the Iran war — the event that motivated this project, whose inclusion would let the motivating crisis confirm the hypothesis by construction.
- Both variants are computed and reported: crisis-excluded (verdict-bearing) and crisis-included (S3, consistency).
- Effective crisis-excluded blocks: B1ex = 2001–2007 (7 obs); B2ex = 2010–2017 (8 obs); B3ex = {2018, 2019, 2022, 2023, 2024, 2025} (6 obs).

**CONFIRM requires all four of:**
- **C1.** Δρ > 0 (crisis-excluded point estimate).
- **C2.** The one-sided 90% bootstrap confidence bound on Δρ excludes zero.
- **C3.** Sign robustness: Δρ > 0 is preserved in every one of the eight drop-one-domain reruns (S1) and in ≥90% of 1,000 Dirichlet-weighted recomputations of the pair average (S2; symmetric Dirichlet, concentration 1, weights on domains, pair weight = product of its domains' weights renormalized).
- **C4.** Consistency: the crisis-included contrast is ≥ 0, and the Mann–Kendall trend statistic on the 10-year rolling series is non-negative.

**DISCONFIRM = anything short of all four.** Mapping to the verbatim commitment: failing C1/C2 is "flat"; failing C3 is "domain-idiosyncratic" (the appearance of co-drain produced by one or two domains, not a cross-domain property); failing C4 is inconsistency that a pre-registered test does not get to argue around. Per §0, disconfirmation withdraws the correlated-drain claim, full stop.

**Precision diagnostic (v0.2 addition, mandatory reporting; review item 1).** So that "flat" is distinguishable from "unpowered" *on the record*, the Phase 3 results memo MUST report, immediately adjacent to the verdict and in every venue where the verdict is reported:
- **D1.** SE_boot(Δρ): the standard deviation of the bootstrap distribution of Δρ from the §5 procedure.
- **D2.** The two-sided 90% bootstrap percentile interval on Δρ (5th to 95th percentile) and its width.
- **D3.** The minimum detectable effect at 80% power for the frozen one-sided 90% criterion: **MDE₈₀ = (z₀.₉₀ + z₀.₈₀) × SE_boot = 2.123 × SE_boot**, all constants fixed now.
- **D4.** The fixed reporting sentence, verbatim: *"Withdrawal per §0 occurs regardless of this diagnostic; the MDE records whether the instrument could have seen an effect of the size a reader considers meaningful."*

The diagnostic carries no verdict weight, defines no benchmark effect size, and creates no intermediate outcome: a disconfirming result withdraws the claim whether the record shows "flat and well-powered" or "flat and unpowered." Its function is to make the record honest about which of those it is, for the reader and for any future P6-based re-entry attempt.

**Power note, stated against interest and in advance.** With 6–8 observations per crisis-excluded block, individual pairwise correlations carry standard errors near 0.4; averaging over 28 non-independent pairs narrows but does not tame this. The confirm bar is therefore demanding, and an indeterminate-but-suggestive result still withdraws the claim. This asymmetry is accepted deliberately: it is the price of a pre-commitment written by the compromised party, and it has a corollary a referee should also grant — a result that clears C1–C4 under this little power is correspondingly strong evidence, not a statistical accident to be waved off.

---

## 7. Sensitivity battery (enumerated, frozen)

- **S1.** Drop-one-domain: eight reruns of the full analysis, each excluding one domain. Feeds C3.
- **S2.** Weighting perturbation: 1,000 Dirichlet draws over domain weights, weighted mean pairwise correlation recomputed per draw. Feeds C3.
- **S3.** Crisis windows in/out: full analysis on both variants. Feeds C4.
- **S4.** Levels instead of first differences (the pre-named non-primary specification). Reported alongside the headline; not verdict-bearing, with one pre-commitment: if the levels version returns a *significantly opposite* sign (negative contrast, one-sided 90% bound excluding zero), that result must be reported in Dossier v3 immediately adjacent to the headline, not in an appendix. Read S4 with the §3.3 disclosure: domain 2 is detrended by construction and biased toward zero contribution in levels.
- **S5.** Spearman in place of Pearson.
- **S6.** 8-year rolling window in place of 10-year (rolling series only; blocks unchanged).
- **S7.** Social-capital swap: union density → GSS social trust (biennial; pairwise-complete handling per §5; 2020–21 mode break noted and those obs already crisis-excluded).
- **S8.** Quarterly companion: domains 1 (SPR monthly), 2 (credit gap quarterly), 3b (debt/GDP quarterly), 5 (saving rate monthly) — the quarterly-native subset, 6 pairs, calendar-quarter frequency, 40-quarter trailing windows, same normalization and differencing logic. Descriptive and power-supporting only; it is also the measurement vehicle for Prediction P6. Its trailing windows unavoidably contain crisis quarters at both P6 endpoints; the ex-crisis variant is reported as an annotation, and no verdict over 2000–2026 rests on S8. Domain 8 (food) is annual-native and does not enter S8.

S1–S3 are gatekeeping (they feed C3/C4). S4–S8 are reported, not gatekeeping. Nothing outside S1–S8 is run against the frozen criteria; anything else computed in Phase 3 is labeled exploratory and carries no evidential status.

---

## 8. Display composite (non-verdict-bearing)

For visualization only, a **weakest-link composite** — the minimum oriented domain z-score across the eight domains at each date — is displayed alongside the cross-domain median. The min-function is the strong form of non-compensatory aggregation (Munda & Nardo 2009): a deep deficit in one buffer cannot be offset by surpluses elsewhere, which is precisely the thesis's own logic and, per the OECD/JRC *Handbook on Constructing Composite Indicators* (Nardo, Saisana, Saltelli et al. 2008), avoids the compensability objection that makes weighted-average composites "owe more to the craftsmanship of the modeller than to universally accepted scientific rules." The composite carries no inferential weight; the test statistic of §5 is the only verdict-bearing quantity.

---

## 9. Out of scope, listed as future work

Structural-break tests (Bai–Perron) on the correlation trajectory; cointegration / common-stochastic-trend analysis of buffer levels; DCC-GARCH dynamic conditional correlations; factor-model decomposition of common vs idiosyncratic drawdown variance; mixed-frequency (MIDAS-class) estimation that would use the quarterly-native series at full resolution. Each is acknowledged as what a full referee process would eventually demand of v0.3+; none is attempted here, and their absence is disclosed rather than papered over.

---

## 10. Novelty (bounded claim)

The contribution claimed is specific: **a rolling measure of cross-domain buffer covariance within one economy over time** (US, 2000–2026). Differentiation against the nearest prior art identified in Phase 1:

- **Gondauri Index (arXiv, 2026):** a diagnostics-first, *static, cross-country* macro-financial resilience composite — ranks countries at a point in time; measures no within-country, time-varying cross-domain covariance.
- **ECB sectoral systemic-risk-buffer composite indicators:** *financial-domain only* — sophisticated on bank-sector risk, no cross-domain reach into strategic reserves, fiscal, health, food, or associational capacity.
- **The static resilience/vulnerability lineage** (Cutter SoVI; Briguglio; FM Global Resilience Index; ND-GAIN): levels-composites ranking places, none measuring time-varying co-movement of buffer stocks.

Standing caveat, carried verbatim in intent from the Execution Handoff (Limit 9): this novelty finding was established within a bounded search. If prior art surfaces, the correct response is to cite and differentiate, not to defend priority.

---

## 11. Data-handling rules binding on Phase 3 (stated now)

Every series enters a CSV in the working directory with a source-URL-and-retrieval-date column per series. At least 10% of transcribed values are double-entered against source in a second pass; discrepancies trigger a full re-check of the affected series. Every data-quality compromise (gap, splice, vintage substitution, aggregation judgment) is logged in the results memo — the memo reports the outcome against §6's criteria *whichever way it goes*, including the §6 precision diagnostic, and if disconfirm, drafts the withdrawal language for Dossier v3. If Phase 3 runs in an environment with direct API access (e.g., Claude Code on the human's machine pulling EIA/FRED/BIS/USDA directly), fetch scripts replace hand transcription and MUST be committed alongside the CSVs; that is strictly better, and Phase 5 audits the scripts. The CSVs, scripts, and memo ship together so the Phase 5 red team can attack the raw material, not a summary of it. **v0.2 addition:** the documentation-based availability and stability claims made in this revision (§3.2, §13 item 1) are re-verified against source at first Phase 3 contact, before any analysis; a failed verification re-opens only the affected decision, via a post-timestamp versioned amendment, and is reported whichever way it cuts.

---

## 12. What a hostile reviewer attacks first (Phase 5 seed; kickoff done-criterion, restored in v0.2)

First: **power as shield.** With 6–8 usable observations per crisis-excluded block, C1–C4 jointly make DISCONFIRM the overwhelmingly likely outcome under most states of the world, and a pre-registration nearly certain to disconfirm buys the framework a cheap credential — "we submitted to a fair test and withdrew" — while P6 keeps the hypothesis alive prospectively; the §6 precision diagnostic makes the unpowered case legible but does not cure it, and a reviewer will add that the authors assigned the verdict to the lowest-powered variant (annual, interpolation-banned, crisis-excluded) while parking the better-powered quarterly companion where it cannot bear a verdict. Second: **blindness that cannot be proven.** The authoring system's training data contain most of these series through early 2026; §2's disclosure is honest, but no procedural firewall can demonstrate that component selection, crisis dates, or block boundaries were not implicitly fitted to remembered trajectories — the mechanical rules narrow that channel; they do not close it. Third: **post hoc criteria.** §3.0 was written in v0.2, after the selection it ratifies, and the saving rate survives on a change-rate clause a reviewer will call drafted-to-fit; the inverse-coupling distinction from procurement is the load-bearing answer and will take fire. Fourth: **construction coupling.** Domain 2 and the corporate fallback share the BIS credit aggregate; if the substitution rule triggers, a nonzero slice of measured co-movement is artifactual by construction, and mandatory drop-corporate reporting mitigates but does not delete it. Fifth: **the crisis set.** E excludes 2008–09, 2020–21, and 2026 but not 2001, and any date-set chosen by a macro-literate author is attackable as favorable; the honest reply — 2001's inclusion sits in B1 and, if anything, biases Δρ against the thesis — should itself be audited rather than taken from us. These five, in this order, are the seed of Phase 5's target list.

---

## 13. Change log v0.1 → v0.2 (each entry tagged: thesis-helping / null-helping / neutral-precision; governing rule: every change must increase discriminating power, never merely change which outcome is likelier)

**Preamble.** Predecessor: Buffer-Correlation Index Spec v0.1, frozen 2026-07-27, never externally timestamped. This is the single authorized pre-timestamp revision, executed against a written review brief resolving exactly seven items (items 1, 2, 5, 6 concern this spec; 3, 4 concern the slate; 7 is output form). The confirm/disconfirm criteria — C1–C4, withdrawal on anything short, no intermediate verdict — are unweakened: C1, C2, C4 are verbatim; C3's thresholds are unchanged and its cardinality rises with the domain count (stricter, see entry 4).

1. **Window evaluation (item 1): backward extension to 1985 or 1990 evaluated and REJECTED; window 2000–2026 retained; block boundaries re-derived mechanically for the retained window and unchanged (difference sample 2001–2026, near-equal thirds 9/8/9).** Documentation findings: most domains are coverable from 1985 (union density's consistent CPS series runs from 1983; BIS, OMB, BEA, AHA/OECD, USDA series all predate 1985), but **corporate is the binding constraint** — GFSR/S&P aggregate coverage before 2000 is zero (the GFSR first appeared in 2002), so any pre-2000 start deterministically triggers the full-window mechanical switch to BIS NFC credit-to-GDP at spec time, installing a construction overlap with domain 2 across the entire window and abandoning the handoff-mandated net-debt/EBITDA measures by construction rather than by data outcome. Grounds for rejection, in order of weight: (a) the verbatim withdrawal commitment and hypothesis [H] are worded over 2000–2026 — the window is part of the claim's referent, and a verdict computed over 1990–2026 could confirm what 2000–2026 disconfirms or vice versa; substituting the verdict's referent is a change to the commitment, which this revision is barred from making. (b) The corporate consequence above. (c) SPR pre-2000 (and pre-2010) first differences are fill-program-dominated — a policy flow, not shock absorption — regime-inconsistent with the release-era series. (d) The power gain is real and is quantified against interest: a 1990 start with mechanical thirds yields crisis-excluded endpoint blocks of roughly 11 and 9 observations (extending the crisis set by {1990, 1991} for the Gulf-War recession) versus the current 7 and 6, cutting the standard error on Δρ by roughly 30% (per-block variance ∝ ~1/(n−3): 1/4 + 1/3 = 0.58 vs 1/8 + 1/6 = 0.29). That gain is declined because it attaches to a different claim: the governing rule requires sharpening the test of the claim as worded, not re-pricing which claim is tested. A non-verdict-bearing descriptive backward companion run: **noted, deferred to post-timestamp versioning.** Direction: **no operative change** (evaluation recorded); the delivered power remedy is entry 2.
2. **§6 precision diagnostic added (item 1):** SE_boot(Δρ), two-sided 90% percentile interval and width, MDE₈₀ = 2.123 × SE_boot, and the fixed reporting sentence, all mandatory alongside the verdict; no benchmark effect size, no verdict weight, withdrawal unaffected either way. Power ground: it converts "flat" vs "unpowered" from an argument into a recorded quantity. Direction: **neutral-precision**, with one second-order effect disclosed: an unpowered-disconfirm record preserves more of the hypothesis's P6 re-entry credibility than a bare disconfirm would — reputational, not verdict-bearing.
3. **§3.0 inclusion criteria made explicit (item 2),** with the post hoc honesty note stated in the section itself. Direction: **neutral-precision.**
4. **Food domain added (item 2):** USDA grain stocks-to-use as domain 8, construction frozen in §3.1. Power ground: pairs rise 21 → 28, improving the averaging precision of ρ̄ and broadening the cross-domain claim the test can license. Direction: **neutral-precision by construction** — the domain was added blind to its historical trajectory, so its effect on Δρ is unknowable at spec time; that unknowability is the point of the freeze, and Phase 5 is directed to audit the addition for hindsight motive like everything else. Disclosed side effect: C3 now requires sign robustness across eight drop-one reruns rather than seven — a marginally stricter confirm bar (null-helping in expectation), accepted.
5. **NERC reserve margin rejected (item 2):** rationale in §3.2 (K3 and K2 failures). Direction: **no change.**
6. **Incumbent seven reaffirmed; saving-rate object-class clarification (item 2):** §3.2 states why domain 5 passes K1 where procurement fails (fixed positive coupling vs inverse-coupled response variable). Direction: **neutral-precision.**
7. **Credit-gap defense written (item 5):** domain 2 retained unchanged; §3.3 gives the object-class answer and discloses the S4 null-helping asymmetry (detrended component biased toward zero in levels). Direction: **neutral-precision.**
8. **§12 hostile-review section restored (item 6):** a kickoff done-criterion the v0.1 session omitted from the frozen file. Direction: **neutral-precision.**
9. **Mechanical propagation of the domain count:** §1 "seven"→"eight" (and annual-native count "three of seven"→"four of eight"); §3.1 table row 8; §4 scope-restriction wording "seven"→"eight"; §5 "21 pairs"→"28 pairs at full eight-domain strength"; §6 C3 "seven"→"eight" reruns and power note "21"→"28 pairs"; §7 S1 "seven"→"eight" reruns, S8 food-exclusion note; §8 "across the eight domains"; §10 "food" added to the ECB-differentiation domain list; §11 USDA added to the API example list. Direction: **neutral** (follows entry 4).
10. **Documentation basis, disclosed:** the availability and stability claims in entries 1 and 4–5 (GFSR first publication; NERC regional reorganizations and margin-definition changes; USDA series span and stability; BLS union-series 1983 CPS basis; SPR fill-program history) rest on program documentation as carried in the review brief and on general methodological documentation; **no series values were examined in this revision session**, which is what keeps the revision legitimate. Each such claim is re-verified at first Phase 3 contact, before analysis, per §11. Direction: **neutral-precision.**
11. **Defects noticed, out of the review brief's scope — noted, deferred to post-timestamp versioning, flagged to Phase 5:** (i) 2001 (dot-com recession) is not in the crisis set E — an asymmetry with 2008/2020 whose repair would touch verdict machinery and was not among the seven items; its present effect, if any, biases against the thesis (it sits in B1). (ii) The verdict is assigned to the lowest-powered (annual) variant while the quarterly companion is non-verdict-bearing — structural, would re-open the primary statistic. (iii) Domain 1's 2001–2009 observations are fill-program-dominated even within the retained window; S1's drop-SPR rerun partially covers this. (iv) The descriptive backward companion from entry 1.

---

**Version: v0.2 · Date frozen: 2026-07-27 · Predecessor: v0.1 (2026-07-27, superseded pre-timestamp)**
No edits after external timestamp; amendments require a new versioned spec citing this one.
